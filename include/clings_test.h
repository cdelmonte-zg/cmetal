/*
 * clings_test.h — compatibility shim (the project was renamed cmetal).
 * Working copies created before the rename still include this name;
 * everything now lives in cmetal_test.h.
 */
#include "cmetal_test.h"
